import SwiftUI

// MARK: - Step 8: SettingsView implementation
// Placeholder — will be implemented in Step 8

struct SettingsView: View {
    var body: some View {
        VStack {
            Text("SpeechToText Settings")
                .font(.title)
            Text("Coming soon...")
                .foregroundColor(.secondary)
        }
        .frame(width: 450, height: 500)
        .padding()
    }
}
